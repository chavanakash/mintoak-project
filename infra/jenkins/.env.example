# Copy to .env (gitignored) and fill in real values before `docker compose up`.

# GitHub PAT with `contents:write` (fine-grained, scoped to mintoak-project) or
# classic `repo` scope. Used only so Jenkins can push image-tag bump commits.
GITHUB_TOKEN=

# Whatever you want the Jenkins UI admin password to be.
JENKINS_ADMIN_PASSWORD=
